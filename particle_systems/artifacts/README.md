# Local artifacts

Training checkpoints, generated samples, plots, and evaluations are written here and intentionally excluded from Git.

Use a unique subdirectory per run. Never resume or launch two processes into the same output directory.
