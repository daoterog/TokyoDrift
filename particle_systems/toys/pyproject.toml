[project]
name = "drifting-toys"
version = "0.1.0"
description = "Standalone toy experiments for one-shot kernel drifting"
requires-python = ">=3.11,<3.13"
dependencies = [
    "matplotlib>=3.10,<4",
    "torch==2.9.0",
]

[tool.uv]
package = false
