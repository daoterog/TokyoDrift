# Local data

Prepared datasets and source downloads are local-only and intentionally excluded from Git.
Run `python -m particle_systems.prepare_data dw4` or `lj13` after creating the project environment.
